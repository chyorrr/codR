export const metadata = {
  title: 'Profile',
  description: 'Your codR combat record: ELO, win rate, streaks and recent matches.',
  robots: { index: false, follow: false },
};

export default function ProfileLayout({ children }) {
  return children;
}
