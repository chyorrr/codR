export const metadata = {
  title: 'Settings',
  description: 'Sound, animation, editor and privacy preferences for codR.',
  robots: { index: false, follow: false },
};

export default function SettingsLayout({ children }) {
  return children;
}
